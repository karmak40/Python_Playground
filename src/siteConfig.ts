// TODO(founder): hello@example.com is an explicit placeholder — swap it for
// a real inbox before launch. Everything that needs a contact address
// (the Contact page, the Pricing waitlist) reads it from here, so updating
// this one line updates all of them.
export const CONTACT_EMAIL = 'hello@example.com'
